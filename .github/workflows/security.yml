name: Security assurance

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  application-security:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.lock
            requirements-security.txt
      - name: Install locked runtime and security tools
        run: python -m pip install --disable-pip-version-check -r requirements.lock -r requirements-security.txt
      - name: Run security checks
        env:
          PYTHON: python
          SECURITY_OUTPUT_DIR: security-artifacts
        run: ./scripts/security_checks.sh
      - name: Install PostgreSQL for isolated runtime checks
        run: sudo apt-get update && sudo apt-get install -y postgresql
      - name: Run fresh-database HTTP and WebSocket checks
        run: PG_BIN="$(pg_config --bindir)" python scripts/test_runtime_postgres.py
      - name: Retain SBOM and scan output
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: r2c-tracker-security-artifacts
          path: security-artifacts/
          if-no-files-found: warn
          retention-days: 30
