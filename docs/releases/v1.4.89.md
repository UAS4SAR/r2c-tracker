# v1.4.89 production release — September 11, 2026

- Release source: `4e79e1c98f890a95e103383eb0b5ee68a81cc9fe`, annotated tag `v1.4.89`, pushed on `project/modularize-coordination`.
- Scope: operating profiles plus the required aircraft/pilot readiness, service reporting, audited flight corrections, legacy compatibility and short-flight import work.
- Full qualification passed: 390 Python tests, runtime/migrations, dependency audit, static security analysis including the new modules, secret scan and SBOM. Four browser-queue tests also passed.
- The secret scanner first identified a fixed synthetic test-session key. It was explicitly allowlisted. A second run required only baseline line-number normalization; that normalization was inspected and committed. Full qualification then passed on the exact tagged commit.
- Staging: `r2c-tracker-staging-00001-hin`, freshly cloned isolated PostgreSQL databases; authenticated staging health/storage/coordination checks passed.
- Image: `us-west1-docker.pkg.dev/r2c-tracker-pilot/cloud-run-source-deploy/r2c-tracker-staging@sha256:0f3ba1ecd93f1974df44d05b225111f3f578a1ce7e5673056b48e00c41ff555b`.
- Production: `r2c-tracker-pilot-00214-waf`, initially deployed at zero traffic. Independent candidate retest passed; activity was idle immediately before promotion; the same image now serves 100% traffic.
- Previous revision: `r2c-tracker-pilot-00212-sur` (v1.4.88).
- Public `/livez` reports v1.4.89; `/readyz` reports both databases ready. `/versions` contains all five release bullets. The operating-profile admin route redirects unauthenticated users to organization login. Candidate release notes and the login redirect were also checked in the browser.
- The production maximum remains one instance. Public Android and Apple build recommendations remain 185; the development-only iPad build is not advertised as a store update.
- No new revision error-level logs were found during immediate production verification. Physical device/field usability remains operator verification.

The paired iPad was separately updated in place to RID2Caltopo 2.2.7 (198), using the existing signing configuration. Installation, launch and queried device metadata passed; no uninstall/reset occurred.

The release was prepared in `/private/tmp/r2c-tracker-v1.4.89` so unrelated local JSON files stayed untouched. Non-secret receipts and logs are retained in the paired RID2Caltopo `outputs/operating-profiles-20260911/release/` folder. Staging cleanup completed: the temporary Cloud Run service, Cloud SQL instance and six ephemeral secrets were deleted. The reusable service account and empty staging bucket remain.
