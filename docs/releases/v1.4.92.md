# v1.4.92 release — September 15, 2026

## Source and scope

- Source commit: `af253f166408d62adc3b6fe50112e87a3d8c131e`; annotated tag `v1.4.92`.
- Commit and tag pushed to `UAS4SAR/r2c-tracker` before deployment; remote branch and peeled tag both verified at the source commit.
- Release branch: `project/modularize-coordination`. Clean deployment checkout: `/private/tmp/r2c-tracker-release-1.4.92`.
- Includes recording file sizes and live source-label refresh; administrator-only adoption reporting; certificate-date fallback for organization tracking; incident briefing preservation and latest operating-profile display. Also reconciles v1.4.91 source and evidence into this branch.
- Packaging includes both new Python modules; static security checks include both. Local `firebase.json` and `r2cOrWsServices.json` were excluded.

## Validation

- Complete qualification passed: 417 unit tests, runtime/HTTP/protected-route/WebSocket checks, additive migration checks, dependency audit, static analysis, tracked-source secret scan, and SBOM.
- JavaScript source-label regression passed.
- Initial qualification encountered concurrent local test-server use on port 18080, leading to a WebSocket 403. Full qualification rerun on isolated port 18192 passed. Updating the shell security script during its initial invocation also disrupted that invocation; the stable script passed syntax and full qualification on retry.
- Secret baseline normalization changed line numbers and generation time only; reviewed, staged, and included in the release commit.
- Android and iOS store-update recommendations both remain 185, matching prior production. No required protocol-version change.

## Mobile installations

- Current-source Android and Apple apps built and installed in place on connected S11U and Ken’s iPad, both version 2.2.8 (240), and launched successfully.
- Android: 1,108 tests passed; full release build successful; installed APK SHA-256 matches the freshly built artifact.
- Apple: signed device build successful; 370 Swift Testing tests passed.
- No uninstall/data reset. Physical video/voice/flight workflows remain untested in this release task.

## Deployment

- Authenticated staging regression passed on `r2c-tracker-staging-00001-ric`. Candidate digest matched the tested staging image: `us-west1-docker.pkg.dev/r2c-tracker-pilot/cloud-run-source-deploy/r2c-tracker-staging@sha256:61772e2e5b350ed731f8a9c7dce99d709ad821c9a29b841038777952d1badec7`.
- Browser review of the first candidate caught missing v1.4.91 history because its already-published tag was absent locally. Fetched the existing tag, refreshed through the normal guarded workflow using validated staging resources, and confirmed both releases in the candidate page. The source commit and v1.4.92 tag were unchanged.
- Independent candidate regression and idle activity gate passed immediately before promotion. Promoted `r2c-tracker-pilot-00222-gaj` to 100 percent production traffic.
- Previous production / rollback revision: `r2c-tracker-pilot-00219-fex`.
- Public `/livez`, `/readyz`, and `/versions` returned HTTP 200; health reported v1.4.92 and both databases ready. Both v1.4.92 and v1.4.91 release entries were confirmed. Initial error-level log query returned no entries.
- Authenticated read-only browser checks passed for the adoption report, standing operating-profile guidance, pilot certificate/optional-training form, and Streams catalog. No live tablets or recordings were advertised during the catalog check, so actual recording-size presentation and playback still need a physical retest.
- Temporary staging Cloud Run service, isolated Cloud SQL instance, and six ephemeral secrets were deleted. Reusable service account and empty staging bucket retained.
- Follow-up production error query returned no entries. Eight sampled successful requests had response times from 0.003 to 0.553 seconds.
